import styles from './loading.module.css';

const Loading = () => {
  return (
    <p className={styles.box}>
      <sub></sub>
      <sub></sub>
      <sub></sub>
    </p>
  );
}

export default Loading;